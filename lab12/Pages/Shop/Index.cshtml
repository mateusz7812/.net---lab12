@page
@model lab12.Pages_Shop.IndexModel

@{
    ViewData["Title"] = "Shop";
}

<h1>Shop</h1>

<div class="row">
    <div class="col-md-4">
        <form method="get">
            <div class="form-group">
                <label name="CategoryId" class="control-label">Category</label>
                <select name="CategoryId" class ="form-control" asp-items="ViewBag.CategoryId" onchange="document.getElementsByTagName('form')[0].submit();">
                    <option selected>All</option>
                </select>
            </div>
        </form>
    </div>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].Money)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].Category)
            </th>
            <th>
                Image
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Article) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Money)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Name)
            </td>
            <td>
                @if(item.ImageName is null){
                    <img height="60px" src="~/image/placeholder.jpg" />

                }else{
                    <img height="60px" src="~/upload/@item.ImageName" />
                }
            </td>
        </tr>
}
    </tbody>
</table>
